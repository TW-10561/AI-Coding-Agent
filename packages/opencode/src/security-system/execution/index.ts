/**
 * Sandbox & Execution Isolation
 *
 * Provides command execution abstraction with support for:
 * - Host execution (direct, unrestricted)
 * - Docker sandbox (isolated, limited resources)
 */

export * from "./sandboxRunner"
